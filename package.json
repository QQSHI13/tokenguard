{
  "name": "tokenguard-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "docs:build": "mdbook build docs/book-en -d public/docs/en && mdbook build docs/book-zh -d public/docs/zh",
    "dev": "npm run docs:build && vite",
    "build": "tsc -b && npm run docs:build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-opener": "^2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.3.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
