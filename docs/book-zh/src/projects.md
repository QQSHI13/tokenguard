# 项目与 API 密钥

**项目**只是一个标签。它让你按来源区分花费和用量，例如 "Cursor"、"Claude Code" 或 "生产机器人"。

**标签密钥**是你在代理配置中设置为 API 密钥的值。Token Guard 会根据这个标签找到项目名，然后在转发请求前从钥匙串中取出真实的服务商密钥。

## 安全

- 标签密钥可以是任意字符串，不需要保密。
- 真实 API 密钥不会出现在代理配置中。
- 如果传入的密钥没有匹配到任何项目，Token Guard 会返回 HTTP 401。
